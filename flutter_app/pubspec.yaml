name: smart_remote
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  android_remote_pro: ^0.0.1
  shared_preferences: ^2.5.4

flutter:
  uses-material-design: true
